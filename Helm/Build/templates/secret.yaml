apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
data:
  config.json: |
    {
        "auths": {
            "https://index.docker.io/v1/": {
                "auth": "xxxxxxxxxxxxxxx"
            }
        }
    }
